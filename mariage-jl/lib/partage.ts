/**
 * Règles du lien de partage du foyer (brief §6). Proposées en attendant
 * l'arbitrage de la question V1-17.
 *
 * Ces constantes vivent ici, et non dans le fichier de route : Next interdit
 * à un fichier `route.ts` d'exporter autre chose que ses méthodes HTTP.
 */
export const JOURS_VALIDITE = 30;
export const OUVERTURES_MAX = 5;
