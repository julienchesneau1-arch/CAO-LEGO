# Copie de transit — à supprimer

Copie de travail du projet **mariage-jl**, conservée ici parce que le dépôt
dédié décidé le 17/09/2026 n'existe pas encore (l'App GitHub de la session n'a
pas le droit de créer un dépôt, HTTP 403) et que le conteneur est éphémère.

Pour clore le transit : créer le dépôt privé `mariage-jl`, y pousser cette
copie, puis `git rm -r --cached mariage-jl && rm -rf mariage-jl`.
